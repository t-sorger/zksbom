use log::info;

// Function for the "get-commitment" command
pub fn get_commitment(product: &str, version: &str) {
    info!("Getting commitment for product: {}, version: {}", product, version);
    // Add your logic here
}
